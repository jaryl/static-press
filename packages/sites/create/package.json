{
  "name": "@static-press/sites-create",
  "version": "1.0.0",
  "description": "Endpoint to create a new site",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild function.js --bundle --outfile=index.js --platform=node --format=esm --target=node18 --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
