{
  "name": "@static-press/sites-manage",
  "version": "1.0.0",
  "description": "API endpoint for all site operations",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild function.js --bundle --outfile=index.js --platform=node --format=esm --target=node18 --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
