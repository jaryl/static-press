{
  "name": "@static-press/sites-update",
  "version": "1.0.0",
  "description": "Endpoint to update a site",
  "private": true,
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild function.js --bundle --outfile=index.js --platform=node --format=esm --target=node18 --minify"
  },
  "devDependencies": {
    "esbuild": "^0.25.2"
  }
}
